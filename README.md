# vue-slides

`vue-slides` is a lightweight slide deck app.

- Built with Vue 3 and Vite.
- Slides are authored as Vue single-file components and grouped into projects automatically.
- The browser UI includes sidebar navigation, keyboard controls, and static-site deployment support.

## Development

```bash
pnpm install
pnpm dev
```
